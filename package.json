{
  "name": "care-relay-vn",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Synthetic, cited teach-back workflow prototype for Vietnamese discharge instructions",
  "scripts": {
    "test": "node --test",
    "serve": "node scripts/serve.mjs",
    "benchmark": "node benchmarks/run.mjs"
  },
  "engines": { "node": ">=20" }
}